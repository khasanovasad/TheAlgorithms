file (GLOB APP_SOURCES RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} *.cpp)
foreach (sourcefile ${APP_SOURCES})
    string(REPLACE ".cpp" "" file ${sourcefile})
    add_executable(${file} ${sourcefile} "Utils.hpp")
endforeach (sourcefile ${APP_SOURCES})
